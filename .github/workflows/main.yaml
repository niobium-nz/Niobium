name: Publish

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    uses: niobium-nz/Niobium.DevOps/.github/workflows/generic_dotnet_build.yml@main
    with:
      pushPackage: true
      buildVersion: "3.0"
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
