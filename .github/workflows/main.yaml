name: Publish

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    uses: niobium-nz/Niobium.Devops/workflows/GenericDotnetBuild.yml@main
    # with:
    #   nugetFeedUrl: 'https://api.nuget.org/v3/index.json'
    #   nugetApiKey: ${{ secrets.NUGET_API_KEY }}
    # secrets:
    #   NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}