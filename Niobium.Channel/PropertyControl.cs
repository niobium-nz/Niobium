namespace Niobium.Channel
{
    public enum PropertyControl
    {
        Label,

        TextField,

        NumberField,

        Dropdown,

        Calendar,

        // for read only, add indicator support on boolean values
        // for action, add more control support, such as double confirm button
        // for list, add more control support, such as table view
    }
}
